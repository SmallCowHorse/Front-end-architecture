var Teacher = /** @class */ (function () {
    function Teacher() {
    }
    return Teacher;
}());
var Student = /** @class */ (function () {
    function Student() {
    }
    return Student;
}());
function startCourse(cls) {
    if (cls instanceof Teacher) {
        // 老师
    }
    if (cls instanceof Student) {
        // 学生
    }
}
